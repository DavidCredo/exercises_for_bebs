// Hier sind ein paar Aufgaben zu Funktionen in Javascript.

// 1. Schreibe eine Funktion, die als Parameter eine Zahl übergeben bekommt (Minuten) und diese in Sekunden konvertiert und zurückgibt.

//console.log(convertToSeconds(3));

// 2. Wenn eine negative Zahl übergeben wird, soll die Funktion die Eingabe automatisch "normalisieren" also positiv darstellen.

//console.log(convertToSeconds(-20));

// 3. Schreibe eine Funktion returnFirstElement(array), die einen Array als Parameter übergeben bekommt (Egal ob int, Strings, ...)
// und das erste Element im Array zurückgibt.

// const arr = [1, 2, 4, 5, 23];
// console.log(returnFirstElement(arr));

// 4. Schreibe eine Funktion isInArray(num, array), die eine Zahl und einen Array mit Zahlen als Parameter übergeben bekommt.
// Die FUnktion soll prüfen, ob die Zahl im Array vorkommt und true oder false zurückgeben.

// const number = 12;
// const array = [15, 14, 44, 41, 42, 12];
// console.log(isInArray(number, array));